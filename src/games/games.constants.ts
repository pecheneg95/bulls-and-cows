enum GAME_STATUS {
  CREATED = 'created',
  PLAYING = 'playing',
  FINISHED = 'finished',
}

enum GAME_RESULT {
  CREATOR_WIN = 'creator_win',
  OPPONENT_WIN = 'opponent_win',
  DRAW = 'draw',
}

export { GAME_STATUS, GAME_RESULT };
